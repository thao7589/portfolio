{
  "name": "reponsive-porfolio",
  "version": "1.0.0",
  "description": "reponsive porfolio website",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "node-sass -w scss/ -o dist/css/ --recursive"
  },
  "author": "Thao Le",
  "license": "MIT",
  "dependencies": {
    "node-sass": "^4.12.0"
  }
}
